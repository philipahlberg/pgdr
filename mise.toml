[settings]
lockfile = true

[tools]
"cargo:cargo-deny"  = { version = "0.18.5" }
"cargo:cargo-insta" = { version = "1.43.2" }
"cargo:taplo-cli"   = { version = "0.10.0" }

[env]
_.file = ".env"

[tasks.build]
run = "cargo build --release --all-features"

[tasks.test]
run = "cargo test --quiet --all-features"

[tasks.fmt]
run = ["cargo +nightly fmt --all --quiet", "taplo fmt --quiet"]

[tasks.clippy]
run = "cargo clippy --quiet -- -D warnings"

[tasks.deny]
run = "cargo deny check advisories bans licenses"

[tasks.check]
run = "cargo check"

[tasks.ci]
depends = ["check", "fmt", "clippy", "deny"]

[tasks.fix]
run = [
  "cargo check --quiet",
  "cargo clippy --quiet --all-targets --fix --allow-dirty --allow-staged --no-deps",
  "cargo +nightly fmt --quiet --all",
  "cargo test --quiet --no-fail-fast --all-features",
  "cargo doc --quiet --no-deps",
]
